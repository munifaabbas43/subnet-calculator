# subnet-calculator
Python CLI tool that calculates subnet details (network address, broadcast, usable hosts, subnet mask) from CIDR notation and splits networks into equal subnets. Built for DACS Batch 56 networking module.
